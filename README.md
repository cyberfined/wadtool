# Wadtool
It is util to edit archive, which looks like a doom wad.

# Usage
```bash
mkdir build
cd build
cmake ..
cmake --build .
cd ../bin
./wadtool
```
# Dependencies
* CMake >= 3.5
* GTK2
* C compiler
